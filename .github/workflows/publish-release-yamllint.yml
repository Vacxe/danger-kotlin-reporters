name: Shellcheck Publish Release

on:
  push:
    tags:
      - 'yamllint*'

jobs:
  publish-release:
    uses: ./.github/workflows/publish-release-workflow.yaml
    with:
      project: yamllint
    secrets: inherit
